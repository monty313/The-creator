# Fable × KAG — intelligent learn-to-learn method

**When:** 2026-08-06 · **PROVEN 2026-08-07**  
**Code:** `the-truth/lineages/adaptive_rl_brain_7_31_26/fable_kag_l2l.py`  
**Identity:** Fable = *method* · Mark = *mind* (`fable5_mark_here_kag`)  
**Goal:** same > 35 → **HIT: same=36** breach 0 · skill = `fire_skill` multi-day family (not day memo)  
**Embryo:** `TEEN_STAGE_same36_fable_kag_fire_skill.pt` · child floor history `9BDCEAAE…`

---

## Fable loop (binding)

```
measure gap (MWT / laws)
  → KAG retrieve (WHAT_WORKS · LEARNING · pt5 · sets)
  → ONE principle intervention (path family)
  → pool labels across ALL days that share fingerprint
  → BC + high KL + award protect (+ freeze trunk)
  → full 50d score
  → KEEP only if same↑ and breach==0
  → remember pattern family (quality only)
```

## Why prior methods failed L2L

| Method | Failure |
|--------|---------|
| One-day DAgger memo | Converts day, pack 35→32 |
| Path head train | Never reaches `score_policy` actions |
| Dial grid self-climb | Blunt global rules; slow; no Mark labels |
| Joint act CE | Craters child geometry |

## Intelligent difference

1. **Skill id = structure fingerprint family**  
   `fire_skill | wait_skill | no_invent` from full_obs cues — never a calendar date.
2. **Multi-day pool**  
   Same motif on many MWT days → one BC batch (true transfer / L2L).
3. **Fable KEEP/REJECT conscience**  
   Pack is the judge; floor sacred.
4. **learn≠copy proxy**  
   act_match high + hold topology collapse → REJECT before pack thrash.
5. **Meta**  
   Choose next family from law histogram + recent REJECT memory (attention surface only).
6. **Optional pattern-guide decode**  
   NN vote on fingerprint without writing embryo (diagnostic free raise).

## Maps to army KAG

| kag_mark / fable5 | Lab |
|-------------------|-----|
| PatternMemory | `FABLE_KAG_PATTERN_MEMORY.jsonl` |
| meta_update_proposal | `choose_family` |
| learn≠copy gate | act_match + hold_topo |
| fable5 measure→KEEP | `run_fable_kag_l2l` |
| ONE recommendation | `kag_fable_brief()` |

## Run

```powershell
cd $truth; $env:PYTHONPATH=".;code"
python -u lineages/adaptive_rl_brain_7_31_26/fable_kag_l2l.py --max-rounds 10 --keep-floor 35
```

## Done

- [x] same > 35 breach 0 → **36**  
- [x] BEST `growth_method=fable_kag_l2l`  
- [x] skill attribution = **fire_skill** path family, not date  
- [ ] same → 50 (continue same recipe on remaining MWT)  

### KEEP evidence (r2)

| | |
|--|--|
| family | fire_skill |
| laws | miss_continuation + ltf_continuation_htf_strong |
| n labels | 262 · days 15 · multi-day fps 4 |
| act_match | 0.832 |
| POST | same **36** mwt 14 breach 0 |
