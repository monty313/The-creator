# Second Brain Wiki (local vault)

Plain Markdown notes owned by Mark. Written by:

- Chat: `remember: <insight>`
- Code: `markos_agents.vault.SecondBrainVault.save`

Structure:

- `index.md` — note index (auto-updated)
- `log.md` — append-only save log
- `notes/` — dated insight files

This is the Army-native answer to “compounding second brain” vaults (without requiring Obsidian or a separate Node stack). Durable JSONL memory still lives in `data/knowledge/{namespace}/MEMORY.jsonl`.
